# R3 Review Journal: architecture-hld

## Round 1

### Expert Findings

| Expert | Severity | Location | Finding |
|--------|----------|----------|---------|
| expert-1 | high | /scrap/cpu-proxy/nixl/ep-integ-plan.md:23 | The plan still presents a single-node HT fallback with UCX_TLS changes as a runnable proxy validation option, and repeats the fallback run instruction at ep-integ-plan.md:101-103. The HLD rejects known true single-node fallback evidence unless an explicit compatible smoke is added (architecture-hld.md:33-35,197-198), the use cases classify such runs as rejected or inconclusive (use-cases.md:55,67-72), and the current HT test requires num_local_ranks == 8 and num_ranks > 8 (examples/device/ep/tests/test_ht.py:68,493). This leaves maintainers with an invalid validation path in a declared artifact. |
| expert-1 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:29 | The plan keeps UCX-direct versus CPU-proxy BW/latency collection and a comparison table/plot in the Phase 1 todo flow and Phase 1 section (ep-integ-plan.md:29-30,73-75,105-107). The HLD and goal define Phase 1 as correctness-first, require an independent UCX-direct correctness smoke, and place proxy performance/comparison artifacts in follow-on scope (architecture-hld.md:19-31,179-181,200-207; goal.md:8-10,13-21). This makes the completion boundary ambiguous because a follow-on performance artifact is still framed as Phase 1 work in the plan. |
| expert-1 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:26 | The elastic LL plan says to confirm that the suite runs all-RDMA via the nixlGetPtr-null fallback and passes, but it does not require the actor-visible evidence now mandated by the HLD and use cases: proxy backend selection, proxy worker activity, and an explicit all-RDMA fallback signal (architecture-hld.md:117,165-168; use-cases.md:94-110). The current LL kernel branches to RDMA when p2p_ptr_get returns null (examples/device/ep/csrc/kernels/nixl_ep_ll.cu:188-192,257-260,728-793,808-811), and no explicit fallback log/counter was found under examples/device/ep, so a passing LL run could still be inconclusive under the corrected acceptance rules. |
| expert-1 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:62 | The plan cites implementation patterns and methodology in paths that are absent from this checkout: examples/device/pingpong/bench_host.cpp, examples/device/pingpong/meson.build, examples/device/pingpong/bench_kernel.cu, docs/CPU_PROXY_DEMO.md, and docs/PUT_LATENCY_COMPARISON.md (referenced at ep-integ-plan.md:62-63,79-80,107,112). The HLD says absent or stale plan examples should not override the corrected acceptance boundary (architecture-hld.md:209-210), but the plan still uses these missing references as concrete implementation guidance, leaving broken traceability for downstream implementers. |
| expert-2 | medium | {'path': '/scrap/cpu-proxy/nixl/ep-integ-plan.md', 'line': 24} | The plan still presents a single-node HT fallback with UCX_TLS=^cuda_ipc as runnable Phase 1 proxy validation, and repeats that guidance in the run instructions at lines 101-103. Current test_ht.py requires num_local_ranks == 8 and num_ranks > 8, so a single-node run with eight local ranks fails the total-rank assertion while a fewer-local-ranks workaround fails the local-rank assertion. The HLD and goal correctly reject this path as non-accepted evidence, but leaving the stale command path in a declared implementation plan will send implementers toward impossible or inconclusive validation and create avoidable churn. Replace it with the two-node path or an explicitly added HT-compatible smoke before using the plan to drive tasks. |
| expert-3 | high | /scrap/cpu-proxy/nixl/ep-integ-plan.md:23-24,101-103; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/architecture-hld.md:33-35,197-198; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:18,38; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:55,67-72; /scrap/cpu-proxy/nixl/examples/device/ep/tests/test_ht.py:68,493 | The plan still presents a true single-node HT fallback using fewer local ranks and/or UCX_TLS=^cuda_ipc as an actionable proxy validation option. The current HLD, goal, and UC-2 require a runnable HT-compatible smoke/topology and reject the known true single-node fallback unless an explicit compatible smoke/test path is added. The current HT test also requires eight local ranks and more than eight total ranks, so a true single-node run either has only eight total ranks or reduces local ranks and violates those constraints. Leaving this option in the plan can drive maintainers toward invalid or inconclusive Phase 1 evidence. |
| expert-3 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:3,20-30,71-75,105-107; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/architecture-hld.md:28,31,117,200-201,206-210; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:8-10,21,27-34,40-41; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:130-149,163-166 | The plan keeps UCX-direct versus CPU-proxy comparison work inside the Phase 1 todo flow and Phase 1 section, while the HLD, goal, and use cases define Phase 1 as correctness-first and require UCX-direct stability as a separate small correctness smoke independent of any deferred performance comparison. This can make a correctness-complete Phase 1 appear incomplete because a follow-on comparison artifact remains framed as Phase 1 scope, while the distinct UCX-direct correctness smoke is not called out as its own plan item. |
| expert-3 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:26-27,67,118-119; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/architecture-hld.md:29,36,117,165-168,194-195; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:20,24; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:94-110,125-128 | The plan says to confirm elastic LL runs all-RDMA through the nixlGetPtr-null fallback, but it does not carry the required evidence shape into the validation task: proxy backend selection, proxy worker activity during LL, and an explicit all-RDMA fallback log or counter. The HLD and use cases classify correctness without this evidence as inconclusive, so the plan can still let a passing LL run be treated as Phase 1 evidence without proving the accepted CPU-proxy all-RDMA path was exercised. |
| expert-3 | low | /scrap/cpu-proxy/nixl/ep-integ-plan.md:115-116; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/architecture-hld.md:35,203-204; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:25,32; /scrap/cpu-proxy/nixl/docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:171-188,205-208 | The plan advises raising timeouts or reducing num_tokens/num_sms if the one-worker proxy model blocks correctness validation, but the HLD, goal, and UC-5 require reduced-size criteria to be defined before validation before reduced runs can count as evidence. Without that boundary in the plan, maintainers can downsize after a timeout and later mistake an under-specified run for accepted Phase 1 correctness evidence. |
| expert-4 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:62 | The plan points implementers to proxy wiring and methodology examples that are absent from this checkout: examples/device/pingpong/bench_host.cpp, examples/device/pingpong/meson.build, examples/device/pingpong/bench_kernel.cu, docs/CPU_PROXY_DEMO.md, and docs/PUT_LATENCY_COMPARISON.md. The live repository layout has the EP example under examples/device/ep and proxy publish/config examples under test/gtest/device_api, so these stale paths break traceability to established project patterns. |
| expert-4 | high | /scrap/cpu-proxy/nixl/ep-integ-plan.md:103 | The plan still recommends a single-node HT fallback using fewer local ranks and/or UCX_TLS=^cuda_ipc, but the current HT harness asserts num_local_ranks == 8 and num_ranks > 8. This conflicts with the goal/use-case/HLD contract that the known true single-node fallback is invalid Phase 1 evidence unless an explicit compatible smoke/test path is added. |
| expert-4 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:27 | The elastic LL todo only requires that the suite runs all-RDMA and passes. The current use-case and HLD contracts require proxy backend selection, proxy worker activity, and an explicit fallback signal, so the plan can still steer implementers toward accepting a correctness-only LL pass without the established evidence interface. |
| expert-4 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:29 | The plan keeps the UCX-direct versus CPU-proxy comparison table/plot as a Phase 1 todo, while the HLD and goal make comparison/performance artifacts follow-ons and require only an independent UCX-direct correctness smoke for Phase 1. Leaving this stale todo as pending risks downstream tasks treating a deferred performance artifact as part of the acceptance gate. |
| expert-6 | high | /scrap/cpu-proxy/nixl/ep-integ-plan.md:23 | The plan still lists HT validation as `2-node WORLD_SIZE=2, or single-node fallback with UCX_TLS=^cuda_ipc` and later instructs maintainers to use fewer local ranks and/or UCX_TLS changes for a single-node fallback at lines 101-103. The canonical goal rejects the known true single-node HT fallback unless an explicit compatible smoke/test path is added, and UC-2 says such a topology must be rejected or classified as inconclusive. The current HT test enforces `num_local_ranks == 8` and `num_ranks > 8`, so a true single-node run either has only eight total ranks or fewer than eight local ranks and cannot satisfy the accepted evidence path. |
| expert-6 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:26 | The elastic LL validation todo only says to confirm the suite runs all-RDMA via the `nixlGetPtr` null fallback and passes, and the gap analysis states that elastic runs correctly on the proxy at line 67. The goal, use cases, and HLD require proxy backend selection, proxy worker activity, and an explicit all-RDMA fallback signal before LL correctness can count as Phase 1 evidence. As written, the plan can accept an inferred fallback from correctness alone, which those artifacts classify as inconclusive. |
| expert-6 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:29 | The plan keeps a UCX-direct versus CPU-proxy comparison todo in the frontmatter and a Phase 1 comparison section at lines 105-107. The corrected goal and UC-4 require an independent UCX-direct correctness smoke/regression, while explicitly making performance comparison a follow-on rather than a Phase 1 gate. This makes the Phase 1 completion boundary ambiguous and still omits the simpler standalone UCX-direct stability signal that is actually required. |
| expert-6 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:62 | The plan points implementers at supporting examples and docs that are absent from this checkout: `examples/device/pingpong/bench_host.cpp`, `examples/device/pingpong/meson.build`, `examples/device/pingpong/bench_kernel.cu`, `docs/CPU_PROXY_DEMO.md`, and `docs/PUT_LATENCY_COMPARISON.md` appear across lines 62-63, 79-80, 107, and 112. Those broken references weaken implementation traceability for proxy context publish/clear, proxy linking, memview behavior, comparison methodology, and Phase 2 investigation. |
| expert-6 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:116 | The timeout guidance permits raising `timeout_ms` or reducing `num_tokens`/`num_sms` when the one-worker proxy bottlenecks. The goal, UC-2, UC-5, and HLD all require reduced-size configurations to be defined before validation before they can count as correctness evidence. Without that precondition in the plan, maintainers can create ad hoc downsized runs that pass but remain inconclusive under the declared acceptance boundary. |
| expert-7 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:23 | The HT validation todo still lists a single-node fallback with UCX_TLS=^cuda_ipc, and the run instructions at ep-integ-plan.md:101-103 tell maintainers to force local ranks across the wire with fewer local ranks or UCX_TLS changes. The current goal and use cases reject the known true single-node fallback unless an explicit compatible smoke/test path is added (goal.md:18, use-cases.md:55 and use-cases.md:72), and the HLD repeats that accepted HT evidence must use an accepted smoke/topology rather than the known-invalid fallback (architecture-hld.md:197-198). As a validation data path, this can send maintainers toward evidence that the authoritative artifacts classify as rejected or inconclusive. |
| expert-7 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:26 | The elastic LL todo and gap analysis say to confirm all-RDMA by the nixlGetPtr-null fallback and correctness pass (ep-integ-plan.md:26-27 and ep-integ-plan.md:67), but they do not require the evidence fields that the canonical use case and HLD require: proxy backend selection, proxy worker activity, and an explicit all-RDMA fallback signal (use-cases.md:96-110, architecture-hld.md:117 and architecture-hld.md:165-168). A passing LL run recorded from this plan could therefore lack the data needed to distinguish accepted proxy all-RDMA fallback from an inferred or unproven path. |
| expert-7 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:29 | The plan keeps UCX-direct versus CPU-proxy BW/latency collection and a comparison table/plot as a Phase 1 todo and section (ep-integ-plan.md:29-30 and ep-integ-plan.md:105-107). The corrected goal, use cases, and HLD define Phase 1 as correctness-first, require a separate UCX-direct correctness smoke, and explicitly keep comparison artifacts as follow-ons (goal.md:10, use-cases.md:130-149, architecture-hld.md:31 and architecture-hld.md:200-207). This conflates two different evidence records: the independent UCX-direct stability signal and deferred performance/comparison data. |
| expert-7 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:115 | The plan advises raising timeout_ms or reducing num_tokens/num_sms when the one-worker proxy bottlenecks validation (ep-integ-plan.md:115-116). The goal, use cases, and HLD all require reduced-size criteria to be defined before validation before such runs can count as correctness evidence (goal.md:25, use-cases.md:171-188, architecture-hld.md:35 and architecture-hld.md:203-204). Without carrying that precondition into the plan, the validation record can contain ad hoc reduced runs that are not comparable or accepted Phase 1 evidence. |
| expert-7 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:111 | The Phase 2 investigation proposes returning real peer device pointers and possibly indexing a device array of IPC-mapped peer base pointers (ep-integ-plan.md:111-113), but that plan text does not state the data-safety boundary that the HLD now requires before exposing device-usable peer pointers (architecture-hld.md:37 and architecture-hld.md:206-207). If carried forward literally, the future get_ptr data model would be under-specified for rank, memview, bounds, and authorization scoping. |

_Scanned: 11_18_13_expert-1_findings.json, 11_18_13_expert-2_findings.json, 11_18_13_expert-3_findings.json, 11_18_13_expert-4_findings.json, 11_18_13_expert-5_findings.json, 11_18_13_expert-6_findings.json, 11_18_13_expert-7_findings.json, 11_18_13_expert-8_findings.json_

### Fixer Actions

| Finding | Action | Reason |
|---------|--------|--------|
| Invalid true single-node HT fallback in `ep-integ-plan.md` | Updated the plan todo and run instructions to make the two-node HT path the current accepted route, and to classify true single-node/fewer-local-rank or `UCX_TLS=^cuda_ipc` fallback runs as rejected or inconclusive unless a compatible smoke/test path is added first. | Aligns the plan with the HLD/use-case boundary and current `test_ht.py` constraints requiring 8 local ranks and more than 8 total ranks. |
| Phase 1 UCX-direct versus CPU-proxy comparison still framed as required work | Replaced the Phase 1 comparison todo/section with an independent UCX-direct correctness smoke and moved BW/latency tables/plots to explicitly marked follow-on performance work. | Keeps Phase 1 correctness-first and prevents deferred performance artifacts from becoming an acceptance gate. |
| Elastic LL evidence accepted by inference from correctness | Updated the elastic todo and gap analysis to require proxy backend selection, proxy worker activity, and an explicit all-RDMA fallback signal in addition to correctness. | Prevents a passing LL run from being misclassified as accepted evidence when the intended CPU-proxy all-RDMA fallback path is not proven. |
| Stale or absent pingpong/demo/comparison references | Replaced absent `examples/device/pingpong/*`, `docs/CPU_PROXY_DEMO.md`, and `docs/PUT_LATENCY_COMPARISON.md` references with live repository references under `test/gtest/device_api`, `src/api/gpu/meson.build`, `src/api/cpp/nixl.h`, and `src/core/nixl_agent.cpp`. | Restores traceability to files present in this checkout without using stale examples as implementation authority. |
| Reduced-size timeout workaround lacks pre-validation criteria | Updated the risk guidance to require reduced-size criteria before validation and classify ad hoc downsized runs as inconclusive. | Matches the HLD/use-case requirement that reduced runs only count as evidence when criteria are defined ahead of time. |
| Future proxy `nixlGetPtr` peer-pointer design lacks safety boundary | Updated the Phase 2 todo and section to require rank, memview, bounds, lifetime, and authorization safety boundaries before returning device-usable peer pointers. | Keeps deferred peer-pointer restoration from inheriting an under-specified data-safety model. |
