# R3 Review Journal: use-cases

## Round 1

### Expert Findings

| Expert | Severity | Location | Finding |
|--------|----------|----------|---------|
| expert-1 | high | /scrap/cpu-proxy/nixl/ep-integ-plan.md:23-24,101-103 | The plan still presents a single-node HT fallback using fewer local ranks and/or UCX_TLS=^cuda_ipc as a proxy validation path, but the canonical use cases reject true single-node setups that violate current HT constraints unless an explicit compatible smoke/test path is added. The current HT test enforces num_local_ranks == 8 and num_ranks > 8 at examples/device/ep/tests/test_ht.py:493, so the described single-node fallback cannot satisfy UC-2 as written. |
| expert-1 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:29-30,71-107 | The plan keeps UCX-direct versus CPU-proxy BW/latency collection and a comparison table/plot inside the Phase 1 workstream, while the goal and UC-4/UC-5 define Phase 1 as correctness-first and explicitly separate UCX-direct correctness smoke evidence from deferred comparison artifacts. This can make a correctness-complete Phase 1 appear incomplete because a follow-on performance artifact is still framed as part of the Phase 1 plan. |
| expert-1 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:26-27,67,116-118 | UC-3 requires validation to record proxy backend selection, proxy worker activity, and an explicit all-RDMA fallback log or counter. The plan only says to confirm all-RDMA via the nixlGetPtr null fallback, and the inspected LL kernel/test path branches to nixlPut when p2p_ptr_get returns null without producing an observable fallback signal. As written, an elastic LL pass could still lack the evidence UC-3 says is mandatory. |
| expert-1 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:62-63,79-80,107,112 | The plan cites implementation patterns and methodology in paths that are absent from the current checkout: examples/device/pingpong/bench_host.cpp, examples/device/pingpong/meson.build, examples/device/pingpong/bench_kernel.cu, docs/CPU_PROXY_DEMO.md, and docs/PUT_LATENCY_COMPARISON.md. Those broken references weaken traceability for proxy publish wiring, memview behavior, and comparison methodology. |
| expert-1 | low | /scrap/cpu-proxy/nixl/ep-integ-plan.md:115-116 | The plan suggests reducing num_tokens or num_sms if one-worker proxy validation times out, but UC-2 and UC-5 say timeout-reduction runs are inconclusive unless pre-approved reduced-size criteria have been defined. Without carrying that precondition into the plan, maintainers may treat an under-specified downsized run as Phase 1 correctness evidence. |
| expert-2 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:3,29-30,71-107 | The plan still frames UCX-direct versus CPU-proxy comparison as part of the Phase 1 workstream: the overview says the integration enables an apples-to-apples comparison, the frontmatter has a pending compare todo, and the Phase 1 section ends with a required table/plot. This conflicts with the updated goal and use cases, which make Phase 1 correctness-first and explicitly separate UCX-direct stability evidence from any deferred comparison artifact. A correctness-complete proxy integration could therefore be treated as incomplete because optional comparison work remains presented as Phase 1 scope. |
| expert-2 | high | /scrap/cpu-proxy/nixl/ep-integ-plan.md:23-24,101-103 | The plan still offers a single-node HT fallback using fewer local ranks and/or UCX_TLS changes as a Phase 1 run instruction. The updated goal requires a runnable topology or explicitly added compatible smoke path and says the known true single-node fallback is not valid evidence under current HT constraints; UC-2 likewise rejects true single-node setups that violate local-rank/total-rank constraints unless a compatible smoke/test path has been added. As written, maintainers could produce HT evidence from a topology the current product artifacts classify as invalid or inconclusive. |
| expert-2 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:26-27,67,118-119 | The plan's elastic LL guidance says to confirm the all-RDMA nixlGetPtr-null fallback and correctness, but it does not require the actor-visible evidence shape now specified by UC-3: proxy backend selection, proxy worker activity during LL, and an explicit all-RDMA fallback branch log or counter. This leaves a path where LL passes are accepted based on inferred fallback behavior instead of the explicit fallback evidence required for Phase 1. |
| expert-2 | low | /scrap/cpu-proxy/nixl/ep-integ-plan.md:115-116 | The timeout guidance says to raise timeouts or reduce num_tokens/num_sms if the one-worker proxy bottleneck blocks correctness, but the updated goal and UC-5 require acceptable reduced-size configurations to be defined before reduced runs count as correctness evidence. Without that boundary in the plan, maintainers can downsize validation after a timeout without a pre-approved product threshold for whether the run still proves the intended proxy path. |
| expert-3 | high | ep-integ-plan.md:23-24,101-103; docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:55,67-72; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:18,38; examples/device/ep/tests/test_ht.py:68,493 | The plan still presents a single-node HT fallback using fewer local ranks and/or UCX_TLS=^cuda_ipc as a validation option. The current use cases and goal require a runnable topology or explicit compatible smoke and reject the known true single-node fallback. The current HT test also asserts num_local_ranks == 8 and num_ranks > 8, so a true single-node run either has eight total ranks and fails the >8 assertion or uses fewer local ranks and fails the local-rank assertion. Leaving this fallback in the plan can send implementers toward an impossible or invalid Phase 1 evidence path. |
| expert-3 | medium | ep-integ-plan.md:29-30,71-75,105-107; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:10,21,27-34,40-41; docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:130-149,163-166 | The plan keeps the UCX-direct versus CPU-proxy comparison table/plot inside the Phase 1 section and frontmatter todo list, while the goal and use cases state that Phase 1 is correctness-first and not gated on a performance/comparison artifact. This makes the acceptance boundary ambiguous: a correctness-complete Phase 1 could still appear incomplete because the plan presents comparison output as part of the same workstream instead of a follow-on. |
| expert-3 | medium | ep-integ-plan.md:26-27,67; docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:94-110,125-128; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:20,24 | The use cases require elastic LL validation to record proxy backend selection, proxy worker activity, and an explicit all-RDMA fallback branch log or counter. The plan only says to confirm all-RDMA via the nixlGetPtr null fallback and says elastic runs correctly on the proxy, without defining the observable evidence that acceptance depends on. That gap allows an elastic LL pass to be treated as Phase 1 evidence without proving the required fallback signal. |
| expert-3 | medium | ep-integ-plan.md:115-116; docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:171-188,205-208; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:25,32 | The plan advises raising timeout_ms or reducing num_tokens/num_sms if the one-worker proxy bottlenecks, but the goal and use cases require reduced-size configurations to be defined before validation before they can count as correctness evidence. As written, the plan permits ad hoc downsizing that could later be accepted despite the explicit pre-approval boundary. |
| expert-3 | medium | ep-integ-plan.md:62-63,79-80,107,112 | The plan cites supporting examples and documentation paths that are not present in the current source tree: examples/device/pingpong/bench_host.cpp, examples/device/pingpong/meson.build, examples/device/pingpong/bench_kernel.cu, docs/CPU_PROXY_DEMO.md, and docs/PUT_LATENCY_COMPARISON.md. These broken references weaken traceability for the prescribed proxy wiring, memview behavior, publish wrapper, comparison methodology, and Phase 2 investigation. |
| expert-4 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:62 | The plan cites proxy wiring and methodology examples that are not present in the current repository: examples/device/pingpong/bench_host.cpp, examples/device/pingpong/meson.build, examples/device/pingpong/bench_kernel.cu, docs/CPU_PROXY_DEMO.md, and docs/PUT_LATENCY_COMPARISON.md. Nearby repository layout only contains examples/device/ep under examples/device and the docs root lacks those referenced docs, so the plan's implementation guidance has broken traceability to established project patterns. |
| expert-4 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:24 | The plan still advertises a single-node HT fallback using UCX_TLS=^cuda_ipc and later says to run with fewer local ranks, but the current HT test asserts num_local_ranks == 8 and num_ranks > 8. This conflicts with the newer use-case contract that true single-node setups violating those constraints must be rejected unless an explicit compatible smoke/test path is added. |
| expert-4 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:29 | The plan keeps the UCX-direct versus proxy comparison table/plot as a Phase 1 todo and section, while the goal and use cases define Phase 1 acceptance as correctness-first and explicitly separate UCX-direct stability evidence from any deferred comparison artifact. Leaving the comparison as a Phase 1 plan item risks downstream tasks treating a follow-on performance artifact as a required acceptance gate. |
| expert-4 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:27 | The elastic LL todo and validation text only say to confirm that the suite runs all-RDMA and passes, but the use-case contract requires proxy backend selection, proxy worker activity, and an explicit fallback branch log or counter. This stale plan wording can accept a passing LL run without the evidence shape required by UC-3 and G-008/G-012. |
| expert-5 | high | ep-integ-plan.md:24,101-103; docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:55,65,72; examples/device/ep/tests/test_ht.py:68,493 | The plan still presents a true single-node HT fallback using fewer local ranks and/or UCX_TLS=^cuda_ipc as an accepted proxy validation path. The use cases explicitly state that the known true single-node fallback is not valid unless a compatible smoke/test change makes it runnable, and the current HT test asserts num_local_ranks == 8 plus num_ranks > 8. On one node, eight local ranks gives only eight total ranks, while fewer local ranks violates the local-rank assertion. Keeping this fallback in the plan risks producing inconclusive validation or false Phase 1 evidence instead of driving the required runnable topology or explicit smoke. |
| expert-5 | medium | ep-integ-plan.md:27,67; docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:96,102-110 | The plan says to confirm elastic LL runs all-RDMA because nixlGetPtr returns null, but it does not carry the use-case requirement for observable proxy-backend selection, proxy worker activity, and an explicit fallback branch log/counter into the validation task. A passing LL run without those signals is classified as inconclusive by the use cases, so the plan can still let implementers accept correctness without proving the intended CPU-proxy all-RDMA path was exercised. |
| expert-5 | medium | ep-integ-plan.md:3,23-30,105-107; docs/issues/001-integrate-nixl-ep-with-nixl-cp/use-cases.md:130-149,164-165 | The plan keeps UCX-direct versus proxy comparison work inside the Phase 1 todo flow and does not list a distinct UCX-direct correctness smoke before that optional comparison. The use cases require UCX-direct stability to be proven by an independent small smoke/regression, and explicitly mark comparison-only evidence as missing the Phase 1 stability signal. This mismatch can turn a follow-on performance artifact into a de facto gate while still omitting the simpler regression check that actually satisfies UC-4. |
| expert-5 | medium | ep-integ-plan.md:62-63,79-80,107,112 | The plan cites implementation examples and supporting documentation that are not present in the current source tree: examples/device/pingpong/bench_host.cpp, examples/device/pingpong/meson.build, examples/device/pingpong/bench_kernel.cu, docs/CPU_PROXY_DEMO.md, and docs/PUT_LATENCY_COMPARISON.md. Those broken references make the build/linking and comparison instructions harder to execute and will force downstream implementers to rediscover the intended proxy wiring patterns from other files. |
| expert-6 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:111 | The Phase 2 plan proposes making proxy nixlGetPtr return real peer device pointers and evaluating a device-resolvable array of IPC-mapped peer base pointers. The same section says the proxy currently hands the GPU opaque mem-view IDs, so replacing that with raw peer base pointers would create a data-safety boundary risk unless later design/tasks require the returned pointer to be scoped to the requested remote_mvh/rank and enforce bounds/authorization. If implemented literally, device code could bypass the proxy mem-view abstraction when accessing peer memory. |
| expert-7 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:23 | The plan's HT todo names a 'single-node fallback with UCX_TLS=^cuda_ipc' and the run instructions later say to force the proxy with fewer local ranks or UCX_TLS changes at line 103. The canonical goal requires a runnable topology or an explicitly added HT-compatible proxy smoke, and UC-2 says a true single-node setup that violates current HT constraints must be rejected or classified as inconclusive unless a compatible smoke/test path has been added. Leaving the old fallback as an actionable option makes the validation instructions inaccessible to maintainers because they can follow the plan into a known-invalid run instead of a clearly accepted smoke or two-node path. |

_Scanned: 19_36_49_expert-1_findings.json, 19_36_49_expert-2_findings.json, 19_36_49_expert-3_findings.json, 19_36_49_expert-4_findings.json, 19_36_49_expert-5_findings.json, 19_36_49_expert-6_findings.json, 19_36_49_expert-7_findings.json_

### Fixer Actions

| Finding | Action | Reason |
|---------|--------|--------|
| Consolidated Phase 1 plan-scope findings: invalid true single-node HT fallback, UCX-direct/proxy comparison framed as Phase 1 work, missing elastic LL fallback evidence shape, and unapproved reduced-size timeout guidance. | No edit applied to `use-cases.md` or `goal.md`; both declared task artifacts already encode the corrected acceptance boundaries. | The remaining stale text identified by the review is in `ep-integ-plan.md`, which is not a declared task file for this fixer stop. |
| Broken supporting references and Phase 2 pointer-safety findings in `ep-integ-plan.md`. | Skipped for this fixer stop. | Required edits are in `ep-integ-plan.md` or later architecture/task artifacts; project source and non-declared plan files are outside this stop's writable scope. |
