# R3 Review Journal: goal

## Round 1

### Expert Findings

| Expert | Severity | Location | Finding |
|--------|----------|----------|---------|
| expert-1 | medium | /scrap/cpu-proxy/nixl/ep-integ-plan.md:62 | The plan cites implementation patterns and supporting documentation that are not present in the current source tree: examples/device/pingpong/bench_host.cpp, examples/device/pingpong/meson.build, examples/device/pingpong/bench_kernel.cu, docs/CPU_PROXY_DEMO.md, and docs/PUT_LATENCY_COMPARISON.md. These references appear at lines 62, 63, 79, 80, 107, and 112, creating broken traceability for the prescribed proxy wiring and comparison methodology. |
| expert-1 | high | /scrap/cpu-proxy/nixl/ep-integ-plan.md:103 | The goal allows single-node multi-process validation when it demonstrates the proxy path, and the plan describes a single-node fallback using fewer local ranks. The current HT test cannot run that fallback as described: examples/device/ep/tests/test_ht.py computes WORLD_SIZE from the environment, then asserts num_local_ranks == 8 and num_ranks > 8 at lines 68 and 493. With WORLD_SIZE=1, eight local ranks gives num_ranks=8 and fails the >8 assertion, while fewer local ranks fails the ==8 assertion. |
| expert-2 | medium | ep-integ-plan.md:23-30,73-75,105-107; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:8-10,21,28,33 | The plan keeps the UCX-vs-proxy comparison task inside the Phase 1 workstream and frontmatter todo list, while the goal explicitly says Phase 1 is correctness-first and is not gated on a UCX-direct versus CPU-proxy comparison artifact. As written, a correctness-complete proxy integration could be treated as incomplete because the optional comparison table/plot is still presented as a Phase 1 deliverable. |
| expert-2 | medium | ep-integ-plan.md:20-30,105-107; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:8,14,21 | The only explicit UCX-direct execution evidence appears in the comparison step, but the goal requires the existing UCX-direct EP workflow to remain stable and separately says the comparison artifact is not a Phase 1 gate. If comparison is deferred as intended, the Phase 1 plan no longer contains a clear UCX-direct correctness smoke to prove G-002. |
| expert-2 | medium | ep-integ-plan.md:26-27,67,101-103; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:8,20,24; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal_brainstorm.md:41-43 | The elastic LL validation says to confirm the all-RDMA fallback, but it does not define an observable acceptance signal for proving that fallback was actually used. The goal already calls out that this signal is unresolved and must be defined later, so Phase 1 can currently pass elastic correctness without proving it exercised the accepted proxy all-RDMA behavior. |
| expert-2 | low | ep-integ-plan.md:115-116; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal.md:25,32; docs/issues/001-integrate-nixl-ep-with-nixl-cp/goal_brainstorm.md:45-47 | The plan allows timeout increases or reduced token/SM sizes if the one-worker proxy bottlenecks, but it does not define the minimum reduced configurations that still count as meaningful Phase 1 evidence. That leaves maintainers without a product boundary for distinguishing acceptable correctness-focused downsizing from a run that is too small to validate the intended proxy path. |
| expert-3 | high | examples/device/ep/meson.build:21 | The EP build remains hard-gated to gpu_device_api_backend=ucx, so a proxy-configured build errors out before compiling the module. This diverges from the project-level backend selection pattern in meson.build, where gpu_device_api_is_proxy defines NIXL_GPU_DEVICE_BACKEND_PROXY, and from the proxy linking convention in src/api/gpu/meson.build and test/gtest/meson.build, where nixl_gpu_proxy_lib is linked into targets that need the proxy device symbol. |
| expert-3 | high | examples/device/ep/meson.build:68 | The EP source list and link settings do not include any proxy publish CUDA translation unit, nixl_gpu_proxy_inc_dirs, or gpu_device_api_link_with/nixl_gpu_proxy_lib when the proxy backend is selected. Existing proxy targets link nixl_gpu_proxy_lib directly so nixl_device_proxy.cu participates in the RDC device-link step; without matching that convention, EP would not own the proxy device symbol needed by nixlProxyPublishContext even after the UCX-only gate is removed. |
| expert-3 | high | examples/device/ep/csrc/nixl_ep.cpp:1341 | Buffer::_nixl_agent_init creates nixlAgentConfig without setting enableDeviceProxy, proxyWorkerCount, or proxyChannelCount under NIXL_GPU_DEVICE_BACKEND_PROXY, and it never publishes agent->getProxyDeviceContext() after createBackend or clears the published context during teardown. Existing proxy tests set these config fields, call nixlProxyPublishContext before device use, and call nixlProxyClearContext in teardown; leaving EP on the old agent lifecycle means proxy kernels would observe no published context and device operations would fail instead of exercising the CPU-proxy backend. |
| expert-4 | high | examples/device/ep/meson.build:21 | The EP extension is still hard-gated to gpu_device_api_backend=ucx, so a Phase 1 proxy build fails before any runtime behavior can be validated. The same target also links only nixl_lib at line 153 and does not conditionally include the proxy CUDA translation unit, nixl_gpu_proxy_inc_dirs, or gpu_device_api_link_with/nixl_gpu_proxy_lib. This leaves the core build artifact unable to select or device-link the CPU-proxy GPU backend described by the plan. |
| expert-4 | high | examples/device/ep/csrc/nixl_ep.cpp:1341 | Buffer::_nixl_agent_init creates nixlAgentConfig without enabling the device proxy or sizing proxyWorkerCount/proxyChannelCount, then creates the UCX backend without publishing agent->getProxyDeviceContext() through nixlProxyPublishContext. Teardown at lines 255-318 likewise never clears the published proxy device context. Since the proxy device API reads a CUDA constant published by nixlProxyPublishContext, EP kernels would have no valid proxy context even if the build gate were removed. |
| expert-4 | medium | ep-integ-plan.md:103 | The plan's single-node fallback is not runnable as written. It says to use fewer local ranks and/or UCX_TLS=^cuda_ipc to exercise the proxy, but examples/device/ep/tests/test_ht.py asserts num_local_ranks == 8 and num_ranks > 8 at lines 68 and 493. On a true single-node run, eight local ranks gives num_ranks=8 and fails the >8 assertion, while fewer local ranks fails the ==8 assertion. That leaves the Phase 1 testability story dependent on two-node execution or an additional test change not captured by the plan. |

_Scanned: 15_22_48_expert-1_findings.json, 15_22_48_expert-2_findings.json, 15_22_48_expert-3_findings.json, 15_22_48_expert-4_findings.json, 15_22_48_expert-5_findings.json_

### Fixer Actions

| Finding | Action | Reason |
|---------|--------|--------|
| expert-1 medium broken plan traceability | Skipped for this artifact-only fix stop. | The referenced file is `ep-integ-plan.md`, which is not a declared task file for this stop. `goal.md` now states that the staged plan is source material and that Phase 1 acceptance is narrower than comparison/optimization work, but this stop does not rewrite undeclared plan artifacts. |
| expert-1 high / expert-4 medium impossible single-node HT fallback | Fixed in `goal.md`. | Replaced the generic single-node validation allowance with a requirement for a runnable topology or explicit compatible smoke/test path, and stated that the known true single-node HT fallback is not valid Phase 1 evidence under current HT constraints. |
| expert-2 medium comparison artifact inside Phase 1 | Fixed in `goal.md`. | Tightened Target, G-009, Assumptions, and Phase 1.5 so UCX-direct versus CPU-proxy comparison artifacts and performance tuning are follow-ons, not Phase 1 acceptance gates. |
| expert-2 medium UCX-direct evidence tied only to comparison | Fixed in `goal.md`. | Added a separate UCX-direct correctness smoke/regression requirement in Target, G-002, Assumptions, and Phase 1 so stability evidence is independent of any deferred performance comparison. |
| expert-2 medium elastic LL fallback signal unresolved | Fixed in `goal.md`. | Changed the fallback language so Phase 1 acceptance requires an explicit observable signal that the accepted all-RDMA fallback was used, with architecture/tasks required to define that signal before acceptance. |
| expert-2 low reduced-size evidence boundary | Fixed in `goal.md`. | Clarified that reduced EP configurations must be defined before validation if defaults time out, and that they count only as correctness evidence rather than performance tuning deliverables. |
| expert-3 high / expert-4 high EP build and proxy lifecycle implementation findings | Skipped for this application-layer stop. | The current stop explicitly prohibits project source edits. These source changes remain implementation-stage work and were not modified in the issue artifacts except as preserved R3 findings. |
