version: '1.0'
artifact: goal
role: verifier
instructions: 'These KB sources were explicitly provided by the human during workflow
  Q&A. Use them as required context for this node. Respect the artifact level: early
  goal/use-case artifacts must translate solution detail into user/domain outcomes,
  while architecture artifacts may use solution detail at the appropriate level.


  Use this as background research. For goal documents, do not copy internal solution
  details directly; translate them to user/domain outcomes.


  Verifier use: approved artifacts are the implementation contract. Use these sources
  to check KB-to-approved-artifact traceability. For KB-dependent findings, cite the
  KB source and the approved artifact or code evidence that carries or violates it.
  If implementation follows approved artifacts but raw KB differs, treat the approved
  artifact as authoritative and report only documented deviations or missing traceability.'
sources:
- type: project_doc
  title: ep-integ-plan.md
  path: ep-integ-plan.md
  required: true
  authority: reference
  reason: user-provided workflow Q&A reference
  scope: project
  inline_allowed: false
  inline_policy: pointer-only
  roles:
  - verifier
